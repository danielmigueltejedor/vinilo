[package]
name = "vinilo-core"
description = "The Apple Music engine: the sidecar contract and the catalog client"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
libc = "0.2"

# NOTE: 0.9 -> 0.10 (Apr 2026) changed the API. Do not trust training data for
# it; check https://docs.rs/mpris-server/0.10 before writing a call.
mpris-server = "0.10"

reqwest.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true
hmac = "0.12"
sha1 = "0.10"
md-5 = "0.10"
